apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: nita
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yaml -v
    kompose.version: 1.22.0 (955b78124)
  creationTimestamp: null
  labels:
    io.kompose.service: webapp
  name: webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: webapp
  strategy: {}
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yaml -v
        kompose.version: 1.22.0 (955b78124)
      creationTimestamp: null
      labels:
        io.kompose.network/nita-network: "true"
        io.kompose.service: webapp
    spec:
      containers:
        - args:
            - ./build-and-test-webapp/wait-for-db.sh
          env:
            - name: DJANGO_LOG_LEVEL
              value: DEBUG
          image: ${CONTAINER_REGISTRY}/nita-webapp:latest
          name: webapp
          readinessProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 5
            failureThreshold: 12
          resources: {}
      restartPolicy: Always
status: {}
